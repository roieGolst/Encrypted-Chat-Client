{
  "name": "encrypted-chat-client",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "prebuild": "npx rimraf ./dist",
    "build": "npx tsc",
    "start": "node ./dist",
    "buildAndStart": "yarn run build && yarn run start"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.2",
    "rimraf": "^3.0.2",
    "tsc": "^2.0.4",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "inquirer": "8.0.0"
  }
}
